// EXPECT: FAIL

// Assigning to an immutable local variable (declared with 'let') should fail semantic checking.
fun @main(%p: i32) : i32 {
  let %x: i32 = 1;
^entry:
  %x = 2; // Should fail: %x is immutable
  ret 0;
}
