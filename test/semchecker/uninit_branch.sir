// EXPECT: FAIL

// Reading a variable that is not initialized on all control flow paths should fail semantic checking (definite initialization).
fun @uninit_br(%c: i32) : i32 {
  let mut %x: i32;
  let %zero: i32 = 0;
^entry:
  br %c > %zero, ^t, ^f;
^t:
  %x = 1;
  br ^exit;
^f:
  // %x not init here
  br ^exit;
^exit:
  ret %x;
}
