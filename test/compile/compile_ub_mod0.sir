// EXPECT: FAIL:UndefinedBehavior
// SKIP: INTERPRETER
// SKIP: SOLVER
// Modulo by zero triggers SIGFPE, caught by the compiler runtime.
fun @main() : i32 {
  let %ten: i32 = 10;
  let mut %zero: i32 = 0;
^entry:
  ret %ten % %zero;
}
