// EXPECT: FAIL:UndefinedBehavior
// SKIP: INTERPRETER
// COMPILER_ARGS: --sym %?missing=3

// Mutated from bubble_sort.sir
// Triggers Rule 17: Navigation through null in compiled code.

fun @main() : i32 {
  sym %?missing : value i32 in [0, 10];
  let mut %p: ptr [4] i32 = null;
  let mut %p_elem: ptr i32 = null;
^entry:
  // Rule 17: ptrindex through null is UB.
  %p_elem = ptrindex %p, 0;
  ret 0;
}
