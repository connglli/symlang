// EXPECT: FAIL:UndefinedBehavior
// INTERP_ARGS: --sym %?n=0
// COMPILER_ARGS: --sym %?n=0

// Division by a symbol bound to 0 is strict UB.
fun @main() : i32 {
  sym %?n: value i32;
  let %ten: i32 = 10;
  let mut %denom: i32 = 0;
^entry:
  %denom = %?n;
  ret %ten / %denom;
}
