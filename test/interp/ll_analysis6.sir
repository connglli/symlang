// EXPECT: PASS
// ARGS: --sym %?a=2
fun @main() : i64 {
  sym %?a : value i64;
  let %b: i64 = 3;
  let %c: i64 = 4;
  let %zero: i64 = 0;
  let mut %diff: i64 = 0;
^entry:
  br %zero != 0, ^then, ^else;
^then:
  ret %?a;
^else:
  %diff = %c - %b;
  br %diff != %zero, ^then, ^merge;
^merge:
  ret %c;
}
