// EXPECT: FAIL:UndefinedBehavior
// Storing through a null pointer must be undefined behavior.
fun @main() : i32 {
  let mut %p: ptr i32 = null;
  let %v: i32 = 7;
^entry:
  store %p, %v;
  ret %v;
}
