// EXPECT: FAIL
// SKIP: COMPILER — contract-form `decl` is interpreter-only; C runtime linker cannot resolve the missing body
// SPEC v0.2.2 §11.3 - contract decl called in assume
decl @ora() : i32 { post ret == 0, "z"; };
fun @main() : i32 {
^entry:
  assume call @ora() == 0;
  ret 0;
}
