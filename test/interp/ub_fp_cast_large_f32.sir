// EXPECT: FAIL:UndefinedBehavior
// SKIP: WASM
// Reason: WASM doesn't support ASan/UBSan style runtime trapping for float-to-int out-of-range casts.
// f32 3.4e38 cast to i32: out of i32 range [-2^31, 2^31-1], UB rule 8
fun @main() : i32 {
  let %a: f32 = 3.4e38;
  let mut %i: i32 = 0;
^entry:
  %i = %a as i32;
  ret %i;
}
