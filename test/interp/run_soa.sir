// EXPECT: PASS
struct @Vec { x: i32; y: i32; z: i32; }
struct @Data { pts: [2] @Vec; }
fun @main(): i32 {
  let mut %d: @Data = { { {1,2,3}, {4,5,6} } };
^entry:
  require %d.pts[0].x == 1, "p0.x";
  require %d.pts[1].z == 6, "p1.z";
  %d.pts[0].y = 99;
  require %d.pts[0].y == 99, "mod";
  ret 0;
}
