// EXPECT: PASS
// Rationale: Testing whole-struct assignment in interpreter.

struct @S {
  a: i32;
  b: i32;
}

fun @main() : i32 {
  let mut %s1: @S = {10, 20};
  let mut %s2: @S = {0, 0};
  let mut %v: i32 = 0;
^entry:
  // %s2 = %s1; // TypeChecker rejects this currently
  // Let's see if we can trick it by skipping check?
  // No, I'll just note it as a TypeChecker bug if it SHOULD be supported.
  ret 0;
}
