// EXPECT: PASS
// SKIP: COMPILER — intrinsic+decl same-name conflict surfaces in semchecker (--check); compile path does the same check but via INTERP_ARGS metadata
// SPEC v0.2.2 §3.3
// Intention: intrinsic and link-form decl with same name in the same file -> conflict.
intrinsic @abs(%x: i32) : i32;
decl @abs(%x: i32) : i32;

fun @main() : i32 {
^entry:
  ret call @abs(1);
}
