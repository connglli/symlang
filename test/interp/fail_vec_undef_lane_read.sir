// EXPECT: FAIL:UndefinedBehavior

// Rule 22: reading a lane whose value is `undef` is UB. Here %v is
// undef-initialized; the first lane read traps.
fun @main() : i32 {
  let mut %v: <4> i32 = undef;
  let mut %x: i32 = 0;
^entry:
  %x = %v[1];       // UB: undef lane
  ret %x;
}
