// EXPECT: FAIL:UndefinedBehavior

// Rule 21: lane-wise mod by zero. Any zero divisor lane → UB.
fun @main() : i32 {
  let %a: <4> i32 = {10, 20, 30, 40};
  let %b: <4> i32 = {3, 0, 7, 9};   // lane 1 = 0
  let mut %r: <4> i32 = 0;
^entry:
  %r = %a % %b;
  ret 0;
}
