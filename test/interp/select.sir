// EXPECT: PASS
// INTERP_ARGS: --sym %?x=10
// COMPILER_ARGS: --sym %?x=10
fun @main() : i32 {
  sym %?x : value i32;
  let %y: i32 = 20;
^entry:
  require select %?x < %y, %?x, %y == 10, "Min check";
  ret 0;
}
