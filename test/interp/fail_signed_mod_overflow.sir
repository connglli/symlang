// EXPECT: FAIL:UndefinedBehavior
// SKIP: WASM
// Reason: WASM doesn't support ASan/UBSan style runtime trapping for signed modulo overflow.
fun @main() : i32 {
  let %min: i32 = -2147483648;
  let %neg1: i32 = -1;
  let mut %res: i32 = 0;
^entry:
  %res = %min % %neg1;
  ret 0;
}
