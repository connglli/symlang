// EXPECT: FAIL:UndefinedBehavior
// v0.2.1 Rule 17: ptrindex through a null pointer is UB.

fun @main() : i32 {
  let mut %p_arr: ptr [4] i32 = null;
  let mut %p_elem: ptr i32 = null;
^entry:
  // %p_arr is null; navigation is UB at the ptrindex site.
  %p_elem = ptrindex %p_arr, 0;
  ret 0;
}
