// EXPECT: FAIL:StaticError

// Vectors cannot appear as struct fields (§3.2 / §3.3 FieldType excludes
// VecType).
struct @S {
  v: <4> i32;     // StaticError: vector field
  tag: i32;
}

fun @main() : i32 {
^entry:
  ret 0;
}
