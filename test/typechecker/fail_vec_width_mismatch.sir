// EXPECT: FAIL:StaticError

// Vector arithmetic requires matching width.
fun @main() : i32 {
  let %a: <4> i32 = 0;
  let %b: <8> i32 = 0;
  let mut %c: <4> i32 = 0;
^entry:
  %c = %a + %b;   // StaticError: <4> i32 vs <8> i32
  ret 0;
}
