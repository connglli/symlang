// EXPECT: FAIL
// [v0.2.2] §6.10: exact parameter-type match required (no implicit conversion).
intrinsic @abs(%x: i64) : i64;
fun @main(%a: i32) : i32 {
  let mut %y: i32 = 0;
^entry:
  %y = call @abs(%a) as i32;
  ret %y;
}
