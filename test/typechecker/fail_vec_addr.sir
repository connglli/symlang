// EXPECT: FAIL:StaticError

// Vectors are not addressable (§2.11 / §6.8.2).
fun @main() : i32 {
  let mut %v: <4> i32 = 0;
  let mut %p: ptr i32 = null;
^entry:
  %p = addr %v;   // StaticError: addr on vector-typed lvalue
  ret 0;
}
