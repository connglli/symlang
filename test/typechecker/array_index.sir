// EXPECT: FAIL

// Using a non-integer type (array) as an array index should fail typechecking.
fun @bad_idx(%a: [10] i32) : i32 {
  let %idx: [2] i32 = undef;
^entry:
  ret %a[%idx]; // Error: index not int
}
