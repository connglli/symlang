// EXPECT: PASS
fun @main(): i32 {
    let mut %v: <4> i32 = {1, 2, 3, 4};
    let %w: <4> i32 = %v;
    // v0.2.1 §5.3: Vector lane access via subscript is supported.
    // Currently this fails to parse in symiri.
    let %lane: i32 = %w[1];
    ^entry:
        ret %lane;
}
