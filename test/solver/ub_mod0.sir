// EXPECT: FAIL:UndefinedBehavior
// SKIP: SOLVER
// SOLVER_ARGS: --main @main --path ^entry

// Modulo by zero is strict UB and should fail.
fun @main() : i32 {
  let %ten: i32 = 10;
  let mut %zero: i32 = 0;
^entry:
  ret %ten % %zero;
}
