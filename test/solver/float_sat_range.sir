// EXPECT: PASS
// INTERP_ARGS: --main @test --sym %?x=1.5
// COMPILER_ARGS: --main @test --sym %?x=1.5
// SOLVER_ARGS: --main @test --path ^entry

fun @test() : f32 {
  sym %?x: value f32;
^entry:
  assume %?x == 1.5;
  require %?x > 1.0;
  require %?x < 2.0;
  ret %?x;
}
