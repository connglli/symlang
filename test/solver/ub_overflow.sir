// EXPECT: FAIL
// ARGS: --main @main --path '^entry' --sym %?a=2147483647
// 32-bit overflow check in solver
fun @main() : i32 {
  sym %?a : value i32;
^entry:
  let %b: i32 = %?a + 1;
  require %b < 0; // If it wrapped, this would be true. But overflow is UB -> UNSAT/Infeasible
  ret 0;
}
