// EXPECT: FAIL
// ARGS: --main @main --path '^entry' --sym %?a=-2147483648

// Subtraction overflow (MIN_INT - 1) is strict UB and should fail.
fun @main() : i32 {
  sym %?a : value i32;
^entry:
  let %b: i32 = %?a - 1;
  ret 0;
}
