// EXPECT: PASS
// SOLVER_ARGS: --main @main --path ^entry
fun @main() : i32 {
  let mut %x: <2> f32 = {1.0, 1.0};
  let mut %y: <2> f32 = {1.0, 1.0};
  let mut %cond: <2> i1 = {undef, undef};
^entry:
  %cond = cmp == %x, %y;
  require %cond[0] == 1;
  require %cond[1] == 1;
  ret 0;
}
