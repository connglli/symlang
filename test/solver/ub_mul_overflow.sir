// EXPECT: FAIL
// ARGS: --main @main --path '^entry' --sym %?a=2147483647

// Multiplication overflow (MAX_INT * 2) is strict UB and should fail.
fun @main() : i32 {
  sym %?a : value i32;
^entry:
  let %b: i32 = %?a * 2;
  ret 0;
}
