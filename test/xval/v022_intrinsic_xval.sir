// EXPECT: PASS
// [v0.2.2] Cross-validation: interpreter vs C backend on intrinsic results.
intrinsic @abs(%x: i32) : i32;
intrinsic @min(%a: i32, %b: i32) : i32;
intrinsic @popcount(%x: i32) : i32;
intrinsic @clz(%x: i32) : i32;
fun @main() : i32 {
  let %a: i32 = -7;
  let %b: i32 = 12;
  let mut %r: i32 = 0;
^entry:
  %r = call @abs(%a) + call @min(%a, %b) + call @popcount(%b) + call @clz(1);
  ret %r;
}
